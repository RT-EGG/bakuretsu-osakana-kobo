<Window x:Class="BakuretsuOsakanaKobo.Spikes.SingleInstanceData.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="爆裂おさかな工房 - 単一起動技術スパイク"
        Width="760"
        Height="360"
        MinWidth="620"
        MinHeight="300"
        Background="#171B21"
        Foreground="White">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" FontWeight="SemiBold" Text="単一起動・IPC検証" />
        <TextBlock x:Name="ProcessText"
                   Grid.Row="1"
                   Margin="0,10,0,18"
                   Foreground="#AFC0D6" />
        <Border Grid.Row="2" Padding="18" Background="#252B34" CornerRadius="8">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="最後に受け取ったファイル" />
                <TextBlock x:Name="PathText"
                           Margin="0,8,0,18"
                           Text="（なし）"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="StatusText" Foreground="#9FD6A5" Text="待機中" />
            </StackPanel>
        </Border>
        <TextBlock Grid.Row="3"
                   Margin="0,16,0,0"
                   Foreground="#AFC0D6"
                   Text="二重起動すると、この画面が前面化されます。" />
    </Grid>
</Window>
