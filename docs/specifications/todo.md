# 爆裂おさかな工房 実装チェックリスト

最終更新日: 2026-08-12
状態: Phase 1確定・Phase 2完了・Phase 3完了・Phase 4進行中
対応仕様: `specs.md` 1.0

## 1. 運用ルール

- `[ ]` 未着手、`[x]` 実装・検証完了を表す。
- 項目へ着手するときは、行末に `進行中` と担当・日付を記録する。
- `BLOCKED(Q-xxx)` は、`open-questions.md` の決定待ちを表す。
- 各項目は、実装、必要な自動テスト、手動受け入れ確認、関連文書更新が完了して初めてチェックする。
- 段階的に小さくリリースする方針と最初のリリース範囲は`Q-001`で決定済み。最初のリリース対象は`specs.md` 2.1を正とする。

## 2. Phase 1 仕様確定

- [x] `P1-001` 段階的リリース方針と最初のリリース範囲を承認する。`Q-001`, 2026-07-27
- [x] `P1-002` 高優先度の未決事項をすべて決定する。2026-07-27
- [x] `P1-003` 中優先度の未決事項をすべて決定する。2026-07-27
- [x] `P1-004` 決定を`specs.md`へ反映し、要確認表記を解消する。2026-07-27
- [x] `P1-005` 本チェックリストと仕様IDの対応をレビューする。2026-07-27
- [x] `P1-006` 開発者が`specs.md`と初回リリース範囲を承認する。2026-07-27

## 3. アプリケーション基盤

- [x] `APP-001` Windows 11でメインウィンドウを1つ表示して起動できる。`APP-001`, `ENV-001`, `ENV-002`, `ENV-003`, `ENV-004`。Releaseビルド警告0・エラー0、実ウィンドウの生成・応答を確認。2026-08-08。
- [x] `APP-002` 同時に動作するアプリプロセスを1つに制限する。`APP-002`, `Q-002`。Windows利用者SIDのハッシュを含む`Local\\` Named Mutexを製品へ統合し、実製品EXEの一次起動と3回の二次起動で同一利用者の一次プロセスだけが残ることを確認。2026-08-12。
- [x] `APP-003` 二重起動時は既存ウィンドウを前面化し、指定ファイルを既存プロセスへ渡す。`APP-002`, `OPEN-033`, `SPIKE-005`, `Q-002`。同一利用者限定Named Pipe、64 KiB上限、2秒読取、5秒再試行、ACK、直列キューを製品実装し、最小化解除・表示・アクティブ化・前面化を要求ごとに実行する。実製品EXEで起動直後の日本語・空白WMV要求、0引数、不正IPC拒否後の正常要求、正常終了を確認。最終フォアグラウンドはテストランナーがフォーカスを取り返すため自動ゲートにせず、同じ前面化処理を用いたPhase 2の実画面確認を正とする。2026-08-12。
- [x] `APP-004` `win-x64`のframework-dependent folderをZIPで提供し、インストーラーなしで配布・起動できる。READMEのHow To Useで.NET 10 Desktop Runtime x64の事前導入、ZIPの展開、起動方法を案内する。`APP-004`, `APP-005`, `SPIKE-006`。製品ZIP、How To Use、最終配布フォルダーの起動・通常終了、ローカル配布監査、同一PCでのアセット照合・ZIP展開・必須文書・319プラグイン確認に合格。クリーン環境確認は開発者判断により初回リリースの必須条件から外し、未検証リスクを受容。2026-08-09。
- [ ] `APP-005` 実行ファイルと同じ場所の`data`フォルダーへ永続データを保存し、メニューから同フォルダーを開ける。`APP-006`, `APP-007`, `SPIKE-007`, `Q-012`。設定保存とメニューを製品実装し、実アプリで`data/settings.json`生成を確認済み。メニューの手動受け入れ確認は未完了。Codex、2026-08-08。
- [x] `APP-008` `data`フォルダーへ書き込めない場合は警告し、永続化なしで再生を継続する。`APP-008`, `Q-012`。完了、Codex、2026-08-11。保存失敗の報告、既存値維持、一時ファイル除去、非モーダル警告を自動検証し、動画プロファイルを書込不能にした実WPFで310%・ミュート再生の継続と終了後の再保存を確認。
- [ ] `APP-006` 壊れた永続データがあっても安全な既定値で起動できる。`ERR-002`, `ERR-003`。設定JSONの破損退避、未知スキーマ退避、既定値復旧を製品実装し、自動・実アプリ検証済み。動画プロファイルとプレイリストの製品実装後に同経路を確認する。Codex、2026-08-08。
- [x] `APP-007` 利用者向けエラーと診断ログを記録できる。`ERR-004`。画面には行動可能な短い非モーダル通知、`data/logs/app.log`には例外・対象パスを含むJSON Linesを記録し、5 MiBで1世代ローテーションする。自動テスト14/14と実アプリの開始・警告・終了ログで確認。2026-08-08。

## 4. 動画オープン

- [x] `OPEN-001` 「ファイル」→「開く」から1ファイルを選択できる。`OPEN-010..012`。製品UIへmp4/wmv単一選択ダイアログを実装し、メニュー項目の有効状態と製品ウィンドウの起動・応答・正常終了を確認。2026-08-09。
- [x] `OPEN-002` ファイル選択をキャンセルしても再生状態が変わらない。`OPEN-012`。ダイアログが`true`以外を返した場合は再生バックエンドを呼ばずに復帰する。2026-08-09。
- [x] `OPEN-003` メインウィンドウへの1ファイルドロップで動画を開ける。`OPEN-020`。完了、Codex、2026-08-12。WindowとLibVLCSharp前景操作面の両方で受け付け、Phase 3承認済みの青いフィードバック後にメニューと同じ排他的オープン経路で自動再生する。実WPFのMP4/WMVでCopy表示、再オープン、自動再生、1.0倍復帰を確認。
- [x] `OPEN-004` メインウィンドウへの複数ファイルドロップでは再生状態を維持し、1ファイルだけ指定するよう案内する。`OPEN-021`, `Q-004`。完了、Codex、2026-08-12。複数・非対応ファイルはバックエンドへ渡さず、短い通知を表示する。実WPFのMP4/WMVで現在パス、一時停止、約24秒の位置、再生速度を維持することを確認。
- [x] `OPEN-005` 空白や日本語を含む1ファイルパスを起動引数で受け取れる。`OPEN-030`, `OPEN-031`。初回MP4と起動直後に届く二次WMVを要求順に既存の安全なオープン経路へ渡し、UTF-8 JSON IPC前後の絶対パス完全一致、自動再生、ミュート固定、正常終了を実製品EXEで確認。2026-08-12。
- [x] `OPEN-006` 2個以上の動画パスが起動引数に指定された場合、通知せず無視して再生状態を維持する。`OPEN-032`, `Q-003`。実製品EXEで複数引数を`ignore-multiple`として記録し、バックエンドへ新しいパスを渡さず、直前のIPC動画プロファイルが正常終了時にも維持されることを確認。2026-08-12。
- [x] `OPEN-007` 有効なファイルを開いた後、自動再生する。`OPEN-001`。WPF `VideoView`実出力・ミュート固定で保証対象MP4/WMVを開き、再生状態と時刻進行を確認。2026-08-09。
- [x] `OPEN-008` テスト動画セットを定義し、保証対象のmp4を映像・音声付きで再生できる。`OPEN-002`, `SPIKE-001`, `Q-005`。H.264/AAC 8本のバックエンド検証、1080p/60fpsと4K/60fpsのWPF実描画、D3D11VA、WASAPI、左右音、同期専用MP4の通常再生・先頭シーク後を確認し、2026-07-30に基本再生ゲート合格。
- [x] `OPEN-009` テスト動画セットを定義し、保証対象のwmvを映像・音声付きで再生できる。`OPEN-002`, `SPIKE-001`, `Q-005`。VC-1/WMA 9 Professional 8本のバックエンド検証、1080p/60fpsのWPF実描画、D3D11VA、WASAPI、左右音、同期専用WMVの通常再生・先頭シーク後を確認し、2026-07-30に1080pまでの基本再生ゲート合格。4K WMVは性能保証外。
- [x] `OPEN-012` 保証範囲外または非対応のコーデックを検出し、エラー表示する。`OPEN-002`, `SPIKE-001`, `Q-005`。製品実装へmp4=`h264`/`mp4a`、wmv=`VC-1`/`WMAP`の許可リストを導入し、HEVC実ファイルの拒否とVP9の単体テストに合格。VFRはコーデックが一致すれば拒否しない。2026-08-09。
- [ ] `OPEN-010` 欠損、アクセス拒否、非対応形式・コーデック、破損を可能な範囲で区別して案内し、読み込み前の状態を適切に維持する。`OPEN-003`, `ERR-001`, `Q-020`。Phase 2スパイク: 偽装MP4は`Parse=Done`・`Play=true`・エラーなし、途中切断も公称5秒に対して約1.77秒で正常終了扱い。製品実装は進行中: 2026-08-09に欠損、アクセス拒否、読取不能、解析失敗、トラック欠損、非対応コーデック、ステージング失敗を構造化エラーとして区別し、HEVC拒否時の既存再生維持をWPF実出力で確認。再生開始後の途中破損を最後の映像・時刻を残す画面内エラーにする作業は未完了。
- [ ] `OPEN-014` プレイリスト連続再生中の読み込み失敗をログへ記録し、次の再生可能項目へ進む。`ERR-001`, `Q-020`
- [ ] `OPEN-011` 利用者がWindowsで設定したファイル関連付け経由で動画を開ける。`OPEN-034`, `SPIKE-008`, `Q-018`
- [ ] `OPEN-013` アプリ内にファイル関連付けの登録・解除機能を実装しない。`OPEN-034`, `Q-018`

## 5. 再生・シーク・時刻

- [x] `CTRL-001` 読込後の再生、一時停止、再開ができる。`CTRL-001..003`。製品バックエンドと1段コントローラーを接続し、WPF実出力・ミュート固定で再生、一時停止中の時刻停止、再開後の時刻進行を確認。2026-08-09。
- [x] `CTRL-002` 再生状態に応じてボタンアイコンが切り替わる。`CTRL-003`。未選択は無効な再生、再生中は一時停止、一時停止・停止中は再生のグリフ、読み上げ名、ツールチップを一元管理し、3状態を単体テスト。2026-08-09。
- [x] `CTRL-003` シークバーが現在位置と全体時間を反映する。`CTRL-010`。UI Dispatcher上の200ms間隔更新で正規化した現在位置を反映。2026-08-09。
- [x] `CTRL-004` シークバー操作で対応する位置へ移動できる。`CTRL-011`。クリック、キーボード、ドラッグの値変更を製品バックエンドへ接続し、ドラッグ中は候補時刻を表示。2026-08-09。
- [x] `CTRL-005` 先頭・末尾付近のシークが範囲外にならない。非有限値を含む正規化境界を自動テストし、WPF実出力・ミュート固定で0%・50%・90%・100%位置を確認。2026-08-09。
- [x] `CTRL-006` 時刻をゼロ埋めした`hh:mm:ss / hh:mm:ss`形式で表示する。`CTRL-012`, `CTRL-013`。負値、通常値、末尾超過、24時間超を単体テスト。2026-08-09。
- [x] `CTRL-007` 読み込み中・長さ不明・シーク不能時に決定済みの無効表示を行う。`CTRL-014`。未選択・読み込み中・長さ不明は`--:--:-- / --:--:--`、シーク不能時は時刻を維持して操作を無効化する状態を単体テスト。2026-08-09。

## 6. 音量とミュート

- [x] `VOL-001` インジケーターをマウス操作して0～500%へ変更できる。`VOL-001..002`。完了、Codex、2026-08-11。スライダーと数値表示を単一PCM/NAudio経路へ接続し、0～500%境界を自動・実WPF検証。
- [x] `VOL-002` 100%を動画本来の最大音量として扱う。`VOL-002`。完了、Codex、2026-08-09。2026-08-11の製品統合では0～500%を単一PCM/NAudio経路とし、100%以下は原音に対する線形音量、100%超だけ先読みリミッターを適用する。
- [x] `VOL-003` 100%超をソフトウェア増幅し、リミッター等でピークを抑制する。`VOL-003`, `SPIKE-003`, `Q-006`。完了、Codex、2026-08-11。Phase 2スパイクで追加0 dBのMP4/WMV 500%実出力を自動・聴感確認。Phase 4製品統合は0～500%の単一PCM/NAudio経路、先読み5 ms、リリース80 ms、100%超-1 dBFS上限として実装し、75/75テスト、ミュート固定MP4/WMV自然終了、性能3条件×5回、発行物監査に合格。製品500%の3秒実音声もWindows音量50%でピーク0.891251、範囲外サンプル・WASAPI失敗0、終了後ミュートを自動確認し、開発者が聴感合格とした。
- [x] `VOL-004` 動画領域上のホイールで1ノッチ5%の音量変更ができ、0～500%を越えない。`VOL-004`。完了、Codex、2026-08-11。実WPFで495→500、上限維持、500→495を確認。
- [x] `VOL-005` ミュートと解除を切り替えられる。`VOL-005`。完了、Codex、2026-08-09。ミュート中も表示音量と調整値を保持する。
- [x] `VOL-006` 動画ごとの音量とミュート状態を、最後の変更から3秒後、動画切替前、正常終了時に保存し、次回復元する。`VOL-006`, `PROF-002`, `Q-011`。完了、Codex、2026-08-11。実WPFで275%・ミュート復元、320%の3秒保存、終了直前180%の即時保存を確認。

## 7. 再生速度

- [x] `SPEED-001` 0.25、0.5、1.0、1.5、2.0倍を切り替えられる。`SPEED-001..003`, `SPIKE-002`。完了、Codex、2026-08-11。Phase 2でMP4/WMVの定常品質を主観合格とし、Phase 4製品のLibVLCバックエンドとWPF UIへ5段階を接続。Releaseテスト111/111、ミュート固定の実WPF MP4/WMVで全段階を確認した。切替直後の約100～1,000 msのPCM供給間隔は既知制約として引き続き許容し、シームレス切替を受け入れ条件に含めない。
- [x] `SPEED-002` 新しく動画を開いたとき1.0倍で開始する。`SPEED-002`。完了、Codex、2026-08-11。2.0倍から同じ動画を再オープンする製品経路でバックエンド値、表示、メニューチェックが1.0倍へ戻ることをMP4/WMVで確認。
- [x] `SPEED-003` 右クリックメニューに速度と現在の選択状態を表示する。`SPEED-003`。完了、Codex、2026-08-11。動画面のコンテキストメニューとコントローラー右端の現在値表示を実装し、未選択・読込中・致命的再生エラー・終了処理中は速度選択を無効化する。
- [x] `SPEED-004` 400msの長押し中だけ2.0倍となり、ボタン解放またはフォーカス喪失時に元の速度へ戻る。`SPEED-004`, `Q-007`。完了、Codex、2026-08-11。純粋なジェスチャー状態とWPF入力・タイマーを分離し、元速度を発動時に保持する。実マウスのMP4/WMVで発動最大432 ms、解放と`WM_ACTIVATEAPP`非アクティブ通知による0.5倍復帰を確認。
- [x] `SPEED-005` 発動前のドラッグ移動では発動せず、発動後のマウス移動では解除しない。`SPEED-005`, `Q-007`。完了、Codex、2026-08-11。Windowsの水平・垂直ドラッグ判定距離を用い、実マウスで発動前移動の取消と、発動後移動を含む2.5秒保持をMP4/WMVとも確認。
- [x] `SPEED-006` 長押し時間を既定400msの一元管理された設定値とし、Phase 3で調整できる。`SPEED-006`, `Q-007`。完了、Codex、2026-08-11。`TemporaryPlaybackRateGesture.HoldDuration`へ一元化し、400 msであることを自動テスト。
- [x] `SPEED-007` 長押しと通常クリック、ダブルクリック、ドラッグ&ドロップが誤発火しない。`SPEED-004..006`。完了、Codex、2026-08-12。通常クリック、ダブルクリック、右クリック、発動前ドラッグは2026-08-11に実マウスで確認済み。ファイルドロップ処理では一時速度状態を明示終了し、実WPFのMP4/WMVで拒否・受理後とも長押し待機・発動なしを確認。

## 8. フルスクリーン

- [x] `FULL-001` 動画領域のダブルクリックでフルスクリーンを切り替えられる。実マウス入力のMP4/WMVで切替と復帰を確認済み。`FULL-001`
- [x] `FULL-002` 右クリックメニューからフルスクリーンを切り替えられる。実右クリックでメニューを開き、切替とチェック状態を確認済み。`FULL-002`
- [x] `FULL-003` メインウィンドウがあるディスプレイでフルスクリーン表示する。製品のPer-Monitor V2設定下で、切替前後のモニターハンドル一致とウィンドウ・モニター実ピクセル境界の完全一致を確認済み。`FULL-003`
- [x] `FULL-004` マウス移動でコントロールを表示し、3秒の無操作後に自動的に隠す。コントローラー上と右クリックメニュー操作中は隠さない。`FULL-004`, `Q-017`。完了、Codex、2026-08-11。LibVLCSharp前景WPFオーバーレイへコントローラーを移し、実マウスのMP4/WMVで早期非表示なし、無操作後の非表示、移動再表示、ホバー中・メニュー中の3秒超表示、離脱・閉鎖後の非表示を確認。
- [x] `FULL-005` 複数ディスプレイおよびDPI差がある環境で表示崩れがない。完了、Codex、2026-08-11。製品と検証ツールを`PerMonitorV2, PerMonitor`で揃え、96 DPIの1920×1080画面と144 DPIの3840×2160・1920×1080画面を製品WPFで順次訪問。各画面でレイアウト寸法、フルスクリーン実ピクセル境界、通常表示への完全復帰をMP4/WMVで確認。
- [x] `FULL-006` `Esc`、`Alt+Enter`、ダブルクリック、右クリックメニューから解除できる。入力部品フォーカス中の`Alt+Enter`、通常・最大化状態への復帰も実入力で確認済み。`FULL-005`, `Q-017`
- [x] `FULL-007` 自動非表示時間を一元管理し、既定3秒とする。`FULL-004`, `Q-017`。完了、Codex、2026-08-11。`FullscreenControlsState.AutoHideDelay`を唯一の設定値とし、単体テストと実時間ゲートで3秒を確認。

## 9. シークサムネイル

- [ ] `THUMB-001` シークバーホバー位置へ240×135の画像と時刻行を8px上にポップアップ表示し、ポップアップはマウス操作を受け取らない。`THUMB-001`, `THUMB-003`。寸法と配置は2026-08-08にPhase 3で開発者承認済み。製品実装は未完了。
- [ ] `THUMB-002` 再生開始を妨げず、動画全体に対する設定割合の間隔でバックグラウンド生成する。`THUMB-002`, `THUMB-004`, `Q-008`。Phase 2スパイク完了: 別MediaPlayer・320px幅で1080p/4Kの100枚取得に合格。4Kは14.9秒・CPU 60.7秒のため逐次・低優先度ワーカーとし、4K/60fps主再生との併走でも映像準備+3.4 ms、シーク悪化なし、D3D11VA維持を確認。Phase 4の製品実装は未着手。
- [ ] `THUMB-003` ホバー位置の生成をバックグラウンド処理より優先する。`THUMB-002`, `Q-008`。Phase 2スパイク: 4K低優先度生成中の77%位置を実取得259 ms、起動込み977 msで取得。
- [ ] `THUMB-004` 生成間隔を既定1.0%、範囲0.25～5.0%、刻み0.25%で「表示」メニューの設定ダイアログから変更し、永続化できる。`THUMB-004`, `Q-008`。設定導線は2026-08-08にPhase 3で開発者承認済み。製品実装は未完了。
- [ ] `THUMB-005` 生成待ち、連続ホバー、動画切替、キャンセルを安全に処理し、動画切替時にキャッシュを破棄する。`THUMB-002`, `THUMB-005`, `Q-008`。Phase 2スパイク: 旧動画生成を60 msで停止し、新動画25%を実取得205 ms、起動込み858 msで取得。
- [ ] `THUMB-006` 長尺動画でも再生操作をブロックしない。`THUMB-002`, `PERF-003`, `SPIKE-004`。Phase 2スパイク: 4K/60fps主再生との併走でも映像準備+3.4 ms、50%/90%シーク悪化なし、D3D11VA維持。
- [ ] `THUMB-007` シークバー左右端でポップアップを幅内へクランプし、画面端でも見切れない。`THUMB-003`。挙動は2026-08-08にPhase 3で開発者承認済み。製品実装は未完了。
- [ ] `THUMB-008` 設定変更を次に開く動画から適用し、UIに推定枚数を表示しない。`THUMB-006`, `Q-008`。挙動は2026-08-08にPhase 3で開発者承認済み。製品実装は未完了。

## 10. キーボードショートカット

- [x] `KEY-001` Spaceで再生・一時停止を切り替える。`KEY-001`。完了、Codex、2026-08-11。動画面へ実キーを送り、MP4/WMVで停止・再開を確認。
- [x] `KEY-002` 左右キーで5秒移動し、先頭・末尾を越えない。`KEY-002..003`。完了、Codex、2026-08-11。境界クランプを単体テストし、MP4/WMVで実キーの前後5秒を確認。
- [x] `KEY-003` 上下キーで速度を一段変更し、上下限を越えない。`KEY-004..005`。完了、Codex、2026-08-11。5段階と上下限を単体テストし、MP4/WMVで実キー入力を確認。
- [x] `KEY-004` Alt+Enterでフルスクリーンを切り替える。入力部品フォーカス中を含む実キー入力で確認済み。`KEY-006`
- [x] `KEY-005` `Alt+Enter`と全画面中の`Esc`は常に扱い、その他は入力要素にフォーカスがある時に標準キー操作を優先する。完了、Codex、2026-08-11。音量スライダーにフォーカスした実キーUpが標準の1%変更となり、再生速度を変えないことをMP4/WMVで確認。
- [x] `KEY-006` ショートカットと機能の対応を一か所で管理し、画面やイベント処理へキーを直書きしない。`KEY-007`, `QUAL-002`。完了、Codex、2026-08-11。`PlaybackShortcutMap`へ全割り当てと5秒幅を集約。

## 11. 動画プロファイル

- [x] `PROF-001` Windowsで大文字・小文字を区別しない正規化絶対パスにより、プロファイルを作成・取得する。`PROF-001`, `Q-009`。完了、Codex、2026-08-11。`Path.GetFullPath`と`OrdinalIgnoreCase`辞書を使用し、版付きJSONの重複・相対パスを破損として復旧する。
- [x] `PROF-008` 動画の移動・改名後は別動画として扱い、以前のプロファイルを自動適用しない。`PROF-001`, `Q-009`。完了、Codex、2026-08-11。大文字小文字違いは同一、別ディレクトリは別動画となる自動テストに合格。
- [x] `PROF-002` 音量とミュート状態を動画ごとに保存し、次回オープン時に復元する。`PROF-002`, `Q-011`。完了、Codex、2026-08-11。再生開始前に保存値をPCM経路へ原子的に引き渡し、失敗した動画切替では元の音量状態を復元する。
- [x] `PROF-010` 自動保存の待ち時間を3秒とし、連続操作時の過剰な書き込みと変更消失を抑える。`VOL-006`, `PROF-002`, `Q-011`。完了、Codex、2026-08-11。開発者指定の3秒を一元管理し、実WPFで遅延保存と終了時保存を確認。
- [x] `PROF-003` 右クリックメニューから現在位置を開始位置として登録する。`PROF-003`, `PROF-004`, `Q-010`。完了、Codex、2026-08-11。登録操作で即時に原子的保存し、情報通知を表示する。実WPFのMP4/WMVで30秒付近の登録とJSON反映を確認。
- [x] `PROF-004` 次回オープン時は登録済み開始位置から、未登録時は00:00:00から自動再生する。`PROF-003`, `Q-010`。完了、Codex、2026-08-11。解析済み動画長で初期位置を解決し、実WPFのMP4/WMVで12秒・15秒の復元を確認。
- [x] `PROF-009` 最終再生位置を自動保存・復元しない。`PROF-003`, `Q-010`。完了、Codex、2026-08-11。登録15秒から45秒へ移動して正常終了後も登録値15秒が維持されることをMP4/WMVで確認。
- [x] `PROF-005` 開始位置をシークバー上に表示する。`PROF-005`。完了、Codex、2026-08-12。Phase 3承認済みの8×20pxオレンジ色マーカーをマウス透過で重ね、登録直後・動画切替・サイズ変更時に座標を更新する。実WPFのMP4/WMVで12秒・30秒・15秒の位置、範囲外時の非表示、シーク幅変更への追従を確認。
- [x] `PROF-006` 動画短縮などで開始位置が範囲外になっても安全に先頭から再生する。`PROF-006`。完了、Codex、2026-08-11。動画長より1秒先の登録値をMP4/WMVで与え、0秒へ復帰することを確認。
- [x] `PROF-007` プロファイルを`data`配下へ保存し、書き込み不能時は警告して保存なしで再生を継続する。`PROF-007`, `Q-012`。完了、Codex、2026-08-11。`video-profiles.json`を同一ディレクトリの一意一時ファイルから原子的置換し、失敗時は旧ファイル維持・一時ファイル除去。実WPFで警告と再生継続を確認。

## 12. プレイリスト

- [ ] `LIST-001` プレイリストを別ウィンドウで表示する。`LIST-001`
- [ ] `LIST-002` 名前のない単一プレイリストを管理し、変更のたびに`data`配下へ自動保存する。`LIST-003`, `Q-013`
- [ ] `LIST-013` 初回のプレイリスト機能に、名前付き複数プレイリストの管理機能を含めない。`LIST-003`, `Q-013`
- [ ] `LIST-003` 欠損ファイルを視覚的に区別する。`LIST-002`
- [ ] `LIST-004` 現在再生中の動画を追加できる。未選択時は操作できない。`LIST-010`
- [ ] `LIST-005` 動画未選択時も、ドロップした複数ファイルを一括追加できる。`LIST-011`
- [ ] `LIST-006` 動画未選択時も、ダイアログで選択した複数ファイルを一括追加できる。`LIST-012`
- [ ] `LIST-007` 同じ動画を複数回登録し、各項目を独立した再生順として扱える。`LIST-004`, `Q-014`
- [ ] `LIST-008` ドラッグ操作で項目を並べ替えられる。`LIST-013`
- [ ] `LIST-009` 選択項目をリストから削除し、元ファイルは削除しない。`LIST-014`
- [ ] `LIST-010` 「再生」操作では常に先頭の再生可能な項目から連続再生を開始する。`LIST-020`, `LIST-022`, `Q-015`
- [ ] `LIST-011` 項目のダブルクリックでは、その項目から連続再生を開始する。`LIST-022`, `Q-015`
- [ ] `LIST-012` 正常終了後に次の再生可能項目へ進み、欠損・読込エラー項目をスキップする。`LIST-021`, `LIST-023`, `Q-015`
- [ ] `LIST-014` ループトグルを表示し、末尾でOFFなら停止、ONなら先頭へ戻る。`LIST-024`, `Q-015`
- [ ] `LIST-015` メインウィンドウから別動画を開いた場合は連続再生を解除する。`LIST-025`, `Q-015`
- [ ] `LIST-016` ループは既定OFFとし、ON/OFF状態を保存して次回起動時に復元する。`LIST-026`, `Q-015`
- [ ] `LIST-017` 再生可能項目がない場合はループせず停止し、一度だけ案内する。`LIST-027`, `Q-015`

## 13. 最近開いたファイル

- [ ] `RECENT-001` 「ファイル」内に最近開いたファイルを最大10件表示する。`RECENT-001..002`。最大10件の永続モデルと製品オープン経路からの記録は完了。メニュー表示は未実装。Codex、2026-08-12。
- [x] `RECENT-002` 再生開始に成功したファイルだけを記録し、同じファイルは重複させず先頭へ移動する。`RECENT-003`, `Q-016`。正規化絶対パスを大文字小文字非区別で重複排除し、最新を先頭へ移して最大10件を`data/recent-files.json`へ原子的保存する。実製品EXEで初回MP4と二次WMVだけが記録され、0引数・複数引数・欠損MP4では変化しないことを確認。2026-08-12。
- [ ] `RECENT-003` 欠損項目を無効表示し、個別に削除できる。欠損項目が2件以上なら、欠損項目だけをすべて履歴から削除できる。`RECENT-004`, `Q-016`。欠損パスを自動削除せず保持する永続モデルは完了。無効表示と削除UIは未実装。Codex、2026-08-12。
- [ ] `RECENT-004` 履歴項目から動画を開いて自動再生できる。`OPEN-001`, `RECENT-001`
- [ ] `RECENT-005` 履歴をすべて消去できる。`RECENT-004`, `Q-016`

## 14. 性能・品質・配布

- [x] `PERF-001` Phase 2で基準PC、代表動画、測定手順を固定し、候補技術を実測して目標値を決める。`PERF-001`, `PERF-002`, `SPIKE-009`, `Q-019`。2026-08-01にWPF実出力の3条件×5回測定が15/15回合格し、開発者承認により性能目標を確定。
- [x] `PERF-002` プロセスコールドで、プロセス開始から操作可能なメインウィンドウまで1500 ms以内を各5回すべて満たす。製品WPFの代表3条件×5回で最大1,060.416 ms、15/15回合格。2026-08-09。
- [x] `PERF-003` オープン要求から映像・音声準備まで1000 ms以内を各5回すべて満たす。製品WPFでVoutと再生時計100ms到達の遅い方を測定し、最大807.766 ms、15/15回合格。音声経路の実出力証跡はPhase 2を維持。2026-08-09。
- [x] `PERF-004` 50%・90%シーク要求からシーク先到達まで1500 ms以内を各5回すべて満たす。製品シークバー経由で50%最大933.536 ms、90%最大998.667 ms、30/30操作合格。2026-08-09。
- [ ] `PERF-005` サムネイル生成と保存処理を再生・UI操作とは別の実行経路で処理し、動画操作をブロックしない。`PERF-003`
- [x] `PERF-006` 速度・音量のバックエンド値が要求から100 ms以内に反映されることを各5回すべて満たす。`PERF-002`。完了、Codex、2026-08-11。製品WPFのUIイベントから実LibVLCバックエンド値までをミュート固定で測定し、MP4は速度最大0.456 ms・音量最大0.281 ms、WMVは速度最大0.427 ms・音量最大0.301 msで各5/5回合格。
- [x] `QUAL-001` 再生バックエンド、永続化、UIを分離し、モックによるUIレビューと各ロジックの単体テストを行える。`QUAL-001`。独立したInfrastructure/Playback/WPF UI構成、`IPlaybackBackend`注入、フェイク実装への交換テスト、製品UIへのLibVLC実装注入を完了。2026-08-09。
- [ ] `QUAL-002` 状態遷移、値変換と境界、永続化、プレイリスト、履歴、破損データ復旧の自動テストが通る。`QUAL-003`
- [ ] `QUAL-003` 代表的なmp4/wmv、破損、非対応、欠損ファイルで手動検証する。Phase 2自動スパイクでHEVC、VFR、VP9、途中切断、偽装MP4の解析・デコード・状態置換挙動を記録済み。製品UIでのエラー表示と状態維持は未実装・未検証。
- [ ] `QUAL-004` クリーンなWindows 11環境で配布物を起動し、必須シナリオを完了できる。これは導入・依存関係・機能の確認であり、最低ハードウェア性能の証明とは分離する。初回リリースでは最低CPU・GPU・メモリを定義しない方針を2026-08-05に確定。2026-08-09に別環境用の自動・対話受け入れ手順とJSON証跡を整備し、同一PCの自動事前検証に合格。現ホストにはWindows Sandboxがないため正式な別環境証跡は未取得。開発者判断により初回リリースの必須条件から外し、環境確保後の追加確認として残す。
- [x] `QUAL-005` ライセンス表記と再現可能なビルド・配布手順を用意する。`v0.1.0`の公開物でLGPL/.NET通知、全ファイルSHA-256台帳、GPL-only除外、319プラグイン監査、x64資産525/525件の来歴、VLC本体・contrib 126件・LibVLCSharp・パッケージングソースを含む対応ソースZIPを再検証。同じGitHub Releaseへバイナリと対応ソースを掲載し、GitHub digestと再ダウンロード後SHA-256の一致を確認。2026-08-09。

## 15. 初回リリース判定

- [x] `REL-001` `specs.md` 2.1で定義した初回最小リリース対象が完了している。クリーン環境確認は開発者承認により免除し、初回後の機能は本判定に含めない。2026-08-09。
- [x] `REL-002` 自動テストがすべて成功している。locked restore、Releaseビルド警告0・エラー0、45/45テスト合格。2026-08-09。
- [x] `REL-003` 製品WPF実ウィンドウでMP4/WMVのオープン・自動再生、再生・一時停止・再開、シーク境界、時刻表示、HEVC拒否と既存再生維持、最終配布フォルダーの起動・通常終了を確認。2026-08-09。
- [x] `REL-004` 性能目標を満たしている。製品WPFの代表3条件×5回で起動、オープン、50%・90%シークが全件閾値内。2026-08-09。
- [x] `REL-005` 重大な既知不具合はなく、クリーン環境未検証、Windows高コントラスト専用配色なし、保証外形式・環境などの残存範囲を計画・検証書・リリースノートへ記録。2026-08-09。
- [x] `REL-006` 開発者が`v0.1.0`、PR経由の`main`反映、GitHub Release公開を承認。2026-08-09。
