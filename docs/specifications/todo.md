# 爆裂おさかな工房 実装チェックリスト

最終更新日: 2026-08-15
状態: Phase 1確定・Phase 2完了・Phase 3完了・Phase 4進行中
対応仕様: `specs.md` 1.0

## 1. 運用ルール

- `[ ]` 未着手、`[x]` 実装・検証完了を表す。
- 項目へ着手するときは、行末に `進行中` と担当・日付を記録する。
- `BLOCKED(Q-xxx)` は、`open-questions.md` の決定待ちを表す。
- 各項目は、実装、必要な自動テスト、手動受け入れ確認、関連文書更新が完了して初めてチェックする。
- 段階的に小さくリリースする方針と最初のリリース範囲は`Q-001`で決定済み。最初のリリース対象は`specs.md` 2.1を正とする。

## 2. Phase 1 仕様確定

- [x] `P1-001` 段階的リリース方針と最初のリリース範囲を承認する。`Q-001`, 2026-07-27
- [x] `P1-002` 高優先度の未決事項をすべて決定する。2026-07-27
- [x] `P1-003` 中優先度の未決事項をすべて決定する。2026-07-27
- [x] `P1-004` 決定を`specs.md`へ反映し、要確認表記を解消する。2026-07-27
- [x] `P1-005` 本チェックリストと仕様IDの対応をレビューする。2026-07-27
- [x] `P1-006` 開発者が`specs.md`と初回リリース範囲を承認する。2026-07-27

## 3. アプリケーション基盤

- [x] `APP-001` Windows 11でメインウィンドウを1つ表示して起動できる。`APP-001`, `ENV-001`, `ENV-002`, `ENV-003`, `ENV-004`。Releaseビルド警告0・エラー0、実ウィンドウの生成・応答を確認。2026-08-08。
- [x] `APP-002` 同時に動作するアプリプロセスを1つに制限する。`APP-002`, `Q-002`。Windows利用者SIDのハッシュを含む`Local\\` Named Mutexを製品へ統合し、実製品EXEの一次起動と3回の二次起動で同一利用者の一次プロセスだけが残ることを確認。2026-08-12。
- [x] `APP-003` 二重起動時は既存ウィンドウを前面化し、指定ファイルを既存プロセスへ渡す。`APP-002`, `OPEN-033`, `SPIKE-005`, `Q-002`。同一利用者限定Named Pipe、64 KiB上限、2秒読取、5秒再試行、ACK、直列キューを製品実装し、最小化解除・表示・アクティブ化・前面化を要求ごとに実行する。実製品EXEで起動直後の日本語・空白WMV要求、0引数、不正IPC拒否後の正常要求、正常終了を確認。最終フォアグラウンドはテストランナーがフォーカスを取り返すため自動ゲートにせず、同じ前面化処理を用いたPhase 2の実画面確認を正とする。2026-08-12。
- [x] `APP-004` `win-x64`のframework-dependent folderをZIPで提供し、インストーラーなしで配布・起動できる。READMEのHow To Useで.NET 10 Desktop Runtime x64の事前導入、ZIPの展開、起動方法を案内する。`APP-004`, `APP-005`, `SPIKE-006`。製品ZIP、How To Use、最終配布フォルダーの起動・通常終了、ローカル配布監査、同一PCでのアセット照合・ZIP展開・必須文書・319プラグイン確認に合格。クリーン環境確認は開発者判断により初回リリースの必須条件から外し、未検証リスクを受容。2026-08-09。
- [x] `APP-005` 実行ファイルと同じ場所の`data`フォルダーへ永続データを保存し、メニューから同フォルダーを開ける。`APP-006`, `APP-007`, `SPIKE-007`, `Q-012`。全永続Repositoryが実行ファイル基準の`data`配下を使い、製品XAMLの「データフォルダーを開く」から同パスをShell実行する結線を監査した。Release出力の`data`を実際にExplorerで開けることを確認し、今回開いたウィンドウだけを閉じた。完了、Codex、2026-08-15。
- [x] `APP-008` `data`フォルダーへ書き込めない場合は警告し、永続化なしで再生を継続する。`APP-008`, `Q-012`。完了、Codex、2026-08-11。保存失敗の報告、既存値維持、一時ファイル除去、非モーダル警告を自動検証し、動画プロファイルを書込不能にした実WPFで310%・ミュート再生の継続と終了後の再保存を確認。
- [x] `APP-006` 壊れた永続データがあっても安全な既定値で起動できる。`ERR-002`, `ERR-003`。共通`PortableJsonStore`の不正JSON・未知スキーマ退避と既定値復旧に加え、設定、動画プロファイル、最近開いたファイル、プレイリストそれぞれの不正文書を退避して安全な既定値または空データへ戻すテストを確認した。Releaseテスト243/243合格。完了、Codex、2026-08-15。
- [x] `APP-007` 利用者向けエラーと診断ログを記録できる。`ERR-004`。画面には行動可能な短い非モーダル通知、`data/logs/app.log`には例外・対象パスを含むJSON Linesを記録し、5 MiBで1世代ローテーションする。自動テスト14/14と実アプリの開始・警告・終了ログで確認。2026-08-08。
- [x] `APP-009` 粗いドット絵のサカナを用いた透過アプリケーションアイコンを、製品EXEと全WPFウィンドウへ設定する。`APP-009`。青・青緑のサカナとオレンジの火花を文字なしで作成し、16、20、24、32、40、48、64、128、256 pxのICOへ格納した。EXEから32 pxアイコンを再抽出して透明背景と識別可能な輪郭を確認し、.NET 10.0.201でReleaseビルド警告0・エラー0、253/253テスト合格。固定SDK 10.0.301での再検証は環境復旧後に行う。完了、Codex、2026-08-15。

## 4. 動画オープン

- [x] `OPEN-001` 「ファイル」→「開く」から1ファイルを選択できる。`OPEN-010..012`。製品UIへmp4/wmv単一選択ダイアログを実装し、メニュー項目の有効状態と製品ウィンドウの起動・応答・正常終了を確認。2026-08-09。
- [x] `OPEN-002` ファイル選択をキャンセルしても再生状態が変わらない。`OPEN-012`。ダイアログが`true`以外を返した場合は再生バックエンドを呼ばずに復帰する。2026-08-09。
- [x] `OPEN-003` メインウィンドウへの1ファイルドロップで動画を開ける。`OPEN-020`。完了、Codex、2026-08-12。WindowとLibVLCSharp前景操作面の両方で受け付け、Phase 3承認済みの青いフィードバック後にメニューと同じ排他的オープン経路で自動再生する。実WPFのMP4/WMVでCopy表示、再オープン、自動再生、1.0倍復帰を確認。
- [x] `OPEN-004` メインウィンドウへの複数ファイルドロップでは再生状態を維持し、1ファイルだけ指定するよう案内する。`OPEN-021`, `Q-004`。完了、Codex、2026-08-12。複数・非対応ファイルはバックエンドへ渡さず、短い通知を表示する。実WPFのMP4/WMVで現在パス、一時停止、約24秒の位置、再生速度を維持することを確認。
- [x] `OPEN-005` 空白や日本語を含む1ファイルパスを起動引数で受け取れる。`OPEN-030`, `OPEN-031`。初回MP4と起動直後に届く二次WMVを要求順に既存の安全なオープン経路へ渡し、UTF-8 JSON IPC前後の絶対パス完全一致、自動再生、ミュート固定、正常終了を実製品EXEで確認。2026-08-12。
- [x] `OPEN-006` 2個以上の動画パスが起動引数に指定された場合、通知せず無視して再生状態を維持する。`OPEN-032`, `Q-003`。実製品EXEで複数引数を`ignore-multiple`として記録し、バックエンドへ新しいパスを渡さず、直前のIPC動画プロファイルが正常終了時にも維持されることを確認。2026-08-12。
- [x] `OPEN-007` 有効なファイルを開いた後、自動再生する。`OPEN-001`。WPF `VideoView`実出力・ミュート固定で保証対象MP4/WMVを開き、再生状態と時刻進行を確認。2026-08-09。
- [x] `OPEN-008` テスト動画セットを定義し、保証対象のmp4を映像・音声付きで再生できる。`OPEN-002`, `SPIKE-001`, `Q-005`。H.264/AAC 8本のバックエンド検証、1080p/60fpsと4K/60fpsのWPF実描画、D3D11VA、WASAPI、左右音、同期専用MP4の通常再生・先頭シーク後を確認し、2026-07-30に基本再生ゲート合格。
- [x] `OPEN-009` テスト動画セットを定義し、保証対象のwmvを映像・音声付きで再生できる。`OPEN-002`, `SPIKE-001`, `Q-005`。VC-1/WMA 9 Professional 8本のバックエンド検証、1080p/60fpsのWPF実描画、D3D11VA、WASAPI、左右音、同期専用WMVの通常再生・先頭シーク後を確認し、2026-07-30に1080pまでの基本再生ゲート合格。4K WMVは性能保証外。
- [x] `OPEN-012` 保証範囲外または非対応のコーデックを検出し、エラー表示する。`OPEN-002`, `SPIKE-001`, `Q-005`。製品実装へmp4=`h264`/`mp4a`、wmv=`VC-1`/`WMAP`の許可リストを導入し、HEVC実ファイルの拒否とVP9の単体テストに合格。VFRはコーデックが一致すれば拒否しない。2026-08-09。
- [x] `OPEN-010` 欠損、アクセス拒否、非対応形式・コーデック、破損を可能な範囲で区別して案内し、読み込み前の状態を適切に維持する。`OPEN-003`, `ERR-001`, `Q-020`。欠損、アクセス拒否、読取不能、解析失敗、トラック欠損、非対応コーデック、ステージング失敗を構造化エラーとして区別する。製品WPFでは偽装MP4を事前拒否して既存パス・902 ms位置を維持し、公称5秒の途中切断を942 msで`playback-ended-early`として検出した。最後の映像・時刻を維持した画面内エラーと診断ログを表示し、再生・シークを無効化して`PlaybackEnded`を通知しないため、プレイリストの誤遷移も防ぐ。完了、Codex、2026-08-15。
- [x] `OPEN-014` プレイリスト連続再生中の読み込み失敗をログへ記録し、次の再生可能項目へ進む。`ERR-001`, `Q-020`。候補オープン中の構造化エラーを利用者通知なしの診断ログへ記録し、失敗インデックスを表示したうえで次候補を直列探索する。実WPF・LibVLCで欠損・非対応項目をスキップし、次の再生可能項目へ進むことを確認済み。完了、Codex、2026-08-15。
- [x] `OPEN-011` 利用者がWindowsで設定したファイル関連付け経由で動画を開ける。`OPEN-034`, `SPIKE-008`, `Q-018`。Windowsの関連付けが渡す1ファイル引数を通常起動・二次起動とも共通`LaunchRequest`から安全なオープン経路へ接続する。実製品EXEで初回MP4と、起動直後の日本語・空白WMVを既存プロセスへ渡して自動再生できることを確認済み。完了、Codex、2026-08-15。
- [x] `OPEN-013` アプリ内にファイル関連付けの登録・解除機能を実装しない。`OPEN-034`, `Q-018`。製品コードとUIにレジストリ変更、関連付け登録・解除経路がないことを監査し、利用者がWindows設定から行う仕様を維持している。完了、Codex、2026-08-15。

## 5. 再生・シーク・時刻

- [x] `CTRL-001` 読込後の再生、一時停止、再開ができる。`CTRL-001..003`。製品バックエンドと1段コントローラーを接続し、WPF実出力・ミュート固定で再生、一時停止中の時刻停止、再開後の時刻進行を確認。2026-08-09。
- [x] `CTRL-002` 再生状態に応じてボタンアイコンが切り替わる。`CTRL-003`。未選択は無効な再生、再生中は一時停止、一時停止・停止中は再生のグリフ、読み上げ名、ツールチップを一元管理し、3状態を単体テスト。2026-08-09。
- [x] `CTRL-003` シークバーが現在位置と全体時間を反映する。`CTRL-010`。UI Dispatcher上の200ms間隔更新で正規化した現在位置を反映。2026-08-09。
- [x] `CTRL-004` シークバー操作で対応する位置へ移動できる。`CTRL-011`。クリック、キーボード、ドラッグの値変更を製品バックエンドへ接続し、ドラッグ中は候補時刻を表示。2026-08-09。
- [x] `CTRL-005` 先頭・末尾付近のシークが範囲外にならない。非有限値を含む正規化境界を自動テストし、WPF実出力・ミュート固定で0%・50%・90%・100%位置を確認。2026-08-09。
- [x] `CTRL-006` 時刻をゼロ埋めした`hh:mm:ss / hh:mm:ss`形式で表示する。`CTRL-012`, `CTRL-013`。負値、通常値、末尾超過、24時間超を単体テスト。2026-08-09。
- [x] `CTRL-007` 読み込み中・長さ不明・シーク不能時に決定済みの無効表示を行う。`CTRL-014`。未選択・読み込み中・長さ不明は`--:--:-- / --:--:--`、シーク不能時は時刻を維持して操作を無効化する状態を単体テスト。2026-08-09。

## 6. 音量とミュート

- [x] `VOL-001` インジケーターをマウス操作して0～500%へ変更できる。`VOL-001..002`。完了、Codex、2026-08-11。スライダーと数値表示を単一PCM/NAudio経路へ接続し、0～500%境界を自動・実WPF検証。
- [x] `VOL-002` 100%を動画本来の最大音量として扱う。`VOL-002`。完了、Codex、2026-08-09。2026-08-11の製品統合では0～500%を単一PCM/NAudio経路とし、100%以下は原音に対する線形音量、100%超だけ先読みリミッターを適用する。
- [x] `VOL-003` 100%超をソフトウェア増幅し、リミッター等でピークを抑制する。`VOL-003`, `SPIKE-003`, `Q-006`。完了、Codex、2026-08-11。Phase 2スパイクで追加0 dBのMP4/WMV 500%実出力を自動・聴感確認。Phase 4製品統合は0～500%の単一PCM/NAudio経路、先読み5 ms、リリース80 ms、100%超-1 dBFS上限として実装し、75/75テスト、ミュート固定MP4/WMV自然終了、性能3条件×5回、発行物監査に合格。製品500%の3秒実音声もWindows音量50%でピーク0.891251、範囲外サンプル・WASAPI失敗0、終了後ミュートを自動確認し、開発者が聴感合格とした。
- [x] `VOL-004` 動画領域上のホイールで1ノッチ5%の音量変更ができ、0～500%を越えない。`VOL-004`。完了、Codex、2026-08-11。実WPFで495→500、上限維持、500→495を確認。
- [x] `VOL-005` ミュートと解除を切り替えられる。`VOL-005`。完了、Codex、2026-08-09。ミュート中も表示音量と調整値を保持する。
- [x] `VOL-006` 動画ごとの音量とミュート状態を、最後の変更から3秒後、動画切替前、正常終了時に保存し、次回復元する。`VOL-006`, `PROF-002`, `Q-011`。完了、Codex、2026-08-11。実WPFで275%・ミュート復元、320%の3秒保存、終了直前180%の即時保存を確認。

## 7. 再生速度

- [x] `SPEED-001` 0.25、0.5、1.0、1.5、2.0倍を切り替えられる。`SPEED-001..003`, `SPIKE-002`。完了、Codex、2026-08-11。Phase 2でMP4/WMVの定常品質を主観合格とし、Phase 4製品のLibVLCバックエンドとWPF UIへ5段階を接続。Releaseテスト111/111、ミュート固定の実WPF MP4/WMVで全段階を確認した。切替直後の約100～1,000 msのPCM供給間隔は既知制約として引き続き許容し、シームレス切替を受け入れ条件に含めない。
- [x] `SPEED-002` 新しく動画を開いたとき1.0倍で開始する。`SPEED-002`。完了、Codex、2026-08-11。2.0倍から同じ動画を再オープンする製品経路でバックエンド値、表示、メニューチェックが1.0倍へ戻ることをMP4/WMVで確認。
- [x] `SPEED-003` 右クリックメニューに速度と現在の選択状態を表示する。`SPEED-003`。完了、Codex、2026-08-11。動画面のコンテキストメニューとコントローラー右端の現在値表示を実装し、未選択・読込中・致命的再生エラー・終了処理中は速度選択を無効化する。
- [x] `SPEED-004` 400msの長押し中だけ2.0倍となり、ボタン解放またはフォーカス喪失時に元の速度へ戻る。`SPEED-004`, `Q-007`。完了、Codex、2026-08-11。純粋なジェスチャー状態とWPF入力・タイマーを分離し、元速度を発動時に保持する。実マウスのMP4/WMVで発動最大432 ms、解放と`WM_ACTIVATEAPP`非アクティブ通知による0.5倍復帰を確認。
- [x] `SPEED-005` 発動前のドラッグ移動では発動せず、発動後のマウス移動では解除しない。`SPEED-005`, `Q-007`。完了、Codex、2026-08-11。Windowsの水平・垂直ドラッグ判定距離を用い、実マウスで発動前移動の取消と、発動後移動を含む2.5秒保持をMP4/WMVとも確認。
- [x] `SPEED-006` 長押し時間を既定400msの一元管理された設定値とし、Phase 3で調整できる。`SPEED-006`, `Q-007`。完了、Codex、2026-08-11。`TemporaryPlaybackRateGesture.HoldDuration`へ一元化し、400 msであることを自動テスト。
- [x] `SPEED-007` 長押しと通常クリック、ダブルクリック、ドラッグ&ドロップが誤発火しない。`SPEED-004..006`。完了、Codex、2026-08-12。通常クリック、ダブルクリック、右クリック、発動前ドラッグは2026-08-11に実マウスで確認済み。ファイルドロップ処理では一時速度状態を明示終了し、実WPFのMP4/WMVで拒否・受理後とも長押し待機・発動なしを確認。

## 8. フルスクリーン

- [x] `FULL-001` 動画領域のダブルクリックでフルスクリーンを切り替えられる。実マウス入力のMP4/WMVで切替と復帰を確認済み。`FULL-001`
- [x] `FULL-002` 右クリックメニューからフルスクリーンを切り替えられる。実右クリックでメニューを開き、切替とチェック状態を確認済み。`FULL-002`
- [x] `FULL-003` メインウィンドウがあるディスプレイでフルスクリーン表示する。製品のPer-Monitor V2設定下で、切替前後のモニターハンドル一致とウィンドウ・モニター実ピクセル境界の完全一致を確認済み。`FULL-003`
- [x] `FULL-004` マウス移動でコントロールを表示し、3秒の無操作後に自動的に隠す。コントローラー上と右クリックメニュー操作中は隠さない。`FULL-004`, `Q-017`。完了、Codex、2026-08-11。LibVLCSharp前景WPFオーバーレイへコントローラーを移し、実マウスのMP4/WMVで早期非表示なし、無操作後の非表示、移動再表示、ホバー中・メニュー中の3秒超表示、離脱・閉鎖後の非表示を確認。
- [x] `FULL-005` 複数ディスプレイおよびDPI差がある環境で表示崩れがない。完了、Codex、2026-08-11。製品と検証ツールを`PerMonitorV2, PerMonitor`で揃え、96 DPIの1920×1080画面と144 DPIの3840×2160・1920×1080画面を製品WPFで順次訪問。各画面でレイアウト寸法、フルスクリーン実ピクセル境界、通常表示への完全復帰をMP4/WMVで確認。
- [x] `FULL-006` `Esc`、`Alt+Enter`、ダブルクリック、右クリックメニューから解除できる。入力部品フォーカス中の`Alt+Enter`、通常・最大化状態への復帰も実入力で確認済み。`FULL-005`, `Q-017`
- [x] `FULL-007` 自動非表示時間を一元管理し、既定3秒とする。`FULL-004`, `Q-017`。完了、Codex、2026-08-11。`FullscreenControlsState.AutoHideDelay`を唯一の設定値とし、単体テストと実時間ゲートで3秒を確認。

## 9. シークサムネイル

- [x] `THUMB-001` シークバーホバー位置へ画像と時刻行を8px上にポップアップ表示し、ポップアップはマウス操作を受け取らない。`THUMB-001`, `THUMB-003`。Phase 3承認済みの240×135外観を製品へ移植して実WPF検証後、`RC1-002`で利用者設定の可変寸法・動画比率追従へ改訂した。完了、Codex、2026-08-15。
- [x] `THUMB-002` 再生開始を妨げず、動画全体に対する設定割合の間隔でバックグラウンド生成する。`THUMB-002`, `THUMB-004`, `Q-008`。主再生とは別のMediaPlayer、320px幅BGRA callback、1件上限の待機キュー、50msの協調待機を持つ逐次ワーカーを製品へ統合した。5%セッションは先頭・末尾を含む21枚をキャッシュし、生成・callback失敗0を確認。主再生併走はPhase 2実測と製品WPFで継続を確認した。完了、Codex、2026-08-15。
- [x] `THUMB-003` ホバー位置の生成をバックグラウンド処理より優先する。`THUMB-002`, `Q-008`。最新1件だけの優先スロットを通常生成の次の1枚へ割り込ませ、実LibVLCで75%位置が通常順3枚目・82.8ms、実WPFで77%ホバーの46.2秒スロットが待機表示から103.6msで画像へ切り替わることを確認。完了、Codex、2026-08-15。
- [x] `THUMB-004` 生成間隔を既定1.0%、範囲0.25～5.0%、刻み0.25%で「表示」メニューの設定ダイアログから変更し、永続化できる。`THUMB-004`, `Q-008`。Phase 3承認済み420×230ダイアログを製品化し、schema 1の`settings.json`へ後方互換な省略可能値として原子的保存する。範囲・刻み外は破損設定として退避・既定復旧し、保存失敗時は旧ファイルを維持して実行中の値を保持する。実WPFで1.25%復元、2.25%保存、キャンセル不変、再読込を確認。完了、Codex、2026-08-15。
- [x] `THUMB-005` 生成待ち、連続ホバー、動画切替、キャンセルを安全に処理し、動画切替時にキャッシュを破棄する。`THUMB-002`, `THUMB-005`, `Q-008`。セッション待機とホバー優先要求を各最新1件に制限し、連続移動時は画像を消して新しいスロットだけを待つ。動画切替では旧生成とキャッシュ参照を置換し、終了時はnative callback停止待ち後にMediaPlayer、bufferの順で破棄する。単体テストと実WPF正常終了で確認。完了、Codex、2026-08-15。
- [x] `THUMB-006` 長尺動画でも再生操作をブロックしない。`THUMB-002`, `PERF-003`, `SPIKE-004`。生成要求はUIスレッドから即時返却し、別MediaPlayerの逐次ワーカーが25msの協調待機を挟んで処理する。Phase 2の4K/60fps併走合格に加え、実60秒MP4の5%間隔20枚生成中も分離した主再生の継続を確認。完了、Codex、2026-08-15。
- [x] `THUMB-007` シークバー左右端でポップアップを幅内へクランプし、画面端でも見切れない。`THUMB-003`。中央基準の配置をシークバー幅へクランプし、実WPFの幅474pxで左0px・右234pxを確認。サイズ変更時は古い配置を閉じる。完了、Codex、2026-08-15。
- [x] `THUMB-008` 設定変更を次に開く動画から適用し、UIに推定枚数を表示しない。`THUMB-006`, `Q-008`。動画オープン成功時に正規化パスと設定間隔を不変セッションへ固定し、設定変更中の現在動画は旧値を維持する。ダイアログには割合と適用時期だけを表示する。実WPFで現在セッション1.25%維持、次回オープン2.25%適用を確認。完了、Codex、2026-08-15。

## 10. キーボードショートカット

- [x] `KEY-001` Spaceで再生・一時停止を切り替える。`KEY-001`。完了、Codex、2026-08-11。動画面へ実キーを送り、MP4/WMVで停止・再開を確認。
- [x] `KEY-002` 左右キーで5秒移動し、先頭・末尾を越えない。`KEY-002..003`。完了、Codex、2026-08-11。境界クランプを単体テストし、MP4/WMVで実キーの前後5秒を確認。
- [x] `KEY-003` 上下キーで速度を一段変更し、上下限を越えない。`KEY-004..005`。完了、Codex、2026-08-11。5段階と上下限を単体テストし、MP4/WMVで実キー入力を確認。
- [x] `KEY-004` Alt+Enterでフルスクリーンを切り替える。入力部品フォーカス中を含む実キー入力で確認済み。`KEY-006`
- [x] `KEY-005` `Alt+Enter`と全画面中の`Esc`は常に扱い、その他は入力要素にフォーカスがある時に標準キー操作を優先する。完了、Codex、2026-08-11。音量スライダーにフォーカスした実キーUpが標準の1%変更となり、再生速度を変えないことをMP4/WMVで確認。
- [x] `KEY-006` ショートカットと機能の対応を一か所で管理し、画面やイベント処理へキーを直書きしない。`KEY-007`, `QUAL-002`。完了、Codex、2026-08-11。`PlaybackShortcutMap`へ全割り当てと5秒幅を集約。

## 11. 動画プロファイル

- [x] `PROF-001` Windowsで大文字・小文字を区別しない正規化絶対パスにより、プロファイルを作成・取得する。`PROF-001`, `Q-009`。完了、Codex、2026-08-11。`Path.GetFullPath`と`OrdinalIgnoreCase`辞書を使用し、版付きJSONの重複・相対パスを破損として復旧する。
- [x] `PROF-008` 動画の移動・改名後は別動画として扱い、以前のプロファイルを自動適用しない。`PROF-001`, `Q-009`。完了、Codex、2026-08-11。大文字小文字違いは同一、別ディレクトリは別動画となる自動テストに合格。
- [x] `PROF-002` 音量とミュート状態を動画ごとに保存し、次回オープン時に復元する。`PROF-002`, `Q-011`。完了、Codex、2026-08-11。再生開始前に保存値をPCM経路へ原子的に引き渡し、失敗した動画切替では元の音量状態を復元する。
- [x] `PROF-010` 自動保存の待ち時間を3秒とし、連続操作時の過剰な書き込みと変更消失を抑える。`VOL-006`, `PROF-002`, `Q-011`。完了、Codex、2026-08-11。開発者指定の3秒を一元管理し、実WPFで遅延保存と終了時保存を確認。
- [x] `PROF-003` 右クリックメニューから現在位置を開始位置として登録する。`PROF-003`, `PROF-004`, `Q-010`。完了、Codex、2026-08-11。登録操作で即時に原子的保存し、情報通知を表示する。実WPFのMP4/WMVで30秒付近の登録とJSON反映を確認。
- [x] `PROF-004` 次回オープン時は登録済み開始位置から、未登録時は00:00:00から自動再生する。`PROF-003`, `Q-010`。完了、Codex、2026-08-11。解析済み動画長で初期位置を解決し、実WPFのMP4/WMVで12秒・15秒の復元を確認。
- [x] `PROF-009` 最終再生位置を自動保存・復元しない。`PROF-003`, `Q-010`。完了、Codex、2026-08-11。登録15秒から45秒へ移動して正常終了後も登録値15秒が維持されることをMP4/WMVで確認。
- [x] `PROF-005` 開始位置をシークバー上に表示する。`PROF-005`。完了、Codex、2026-08-12。Phase 3承認済みの8×20pxオレンジ色マーカーをマウス透過で重ね、登録直後・動画切替・サイズ変更時に座標を更新する。実WPFのMP4/WMVで12秒・30秒・15秒の位置、範囲外時の非表示、シーク幅変更への追従を確認。
- [x] `PROF-006` 動画短縮などで開始位置が範囲外になっても安全に先頭から再生する。`PROF-006`。完了、Codex、2026-08-11。動画長より1秒先の登録値をMP4/WMVで与え、0秒へ復帰することを確認。
- [x] `PROF-007` プロファイルを`data`配下へ保存し、書き込み不能時は警告して保存なしで再生を継続する。`PROF-007`, `Q-012`。完了、Codex、2026-08-11。`video-profiles.json`を同一ディレクトリの一意一時ファイルから原子的置換し、失敗時は旧ファイル維持・一時ファイル除去。実WPFで警告と再生継続を確認。

## 12. プレイリスト

- [x] `LIST-001` プレイリストを別ウィンドウで表示する。`LIST-001`。表示メニューから承認済み680×540・最小560×400の所有ウィンドウを1つだけ開き、閉じた時はメニューチェックを同期する。実WPFで再表示まで確認。完了、Codex、2026-08-13。
- [x] `LIST-002` 名前のない単一プレイリストを管理し、変更のたびに`data`配下へ自動保存する。`LIST-003`, `Q-013`。schema 1の`data/playlist.json`へ追加・複数選択削除・ドラッグ並べ替え・ループ変更を直後に原子的保存し、破損退避・旧ファイル維持・重複を許す順序モデルを実装。実WPFで各変更と再読込を確認。完了、Codex、2026-08-13。
- [x] `LIST-013` 初回のプレイリスト機能に、名前付き複数プレイリストの管理機能を含めない。`LIST-003`, `Q-013`。名前フィールドを持たない単一ドキュメントと単一ウィンドウとして実装。完了、Codex、2026-08-13。
- [x] `LIST-003` 欠損ファイルを視覚的に区別する。`LIST-002`。起動時に存在を評価し、赤系背景・不透明度・「見つかりません」文字で表示する。実WPFで存在2件・欠損1件を確認。完了、Codex、2026-08-13。
- [x] `LIST-004` 現在再生中の動画を追加できる。未選択時は操作できない。`LIST-010`。再生成功時に別ウィンドウへ現在パスを同期し、実WPFでボタン追加と即時保存を確認。完了、Codex、2026-08-13。
- [x] `LIST-005` 動画未選択時も、ドロップした複数ファイルを一括追加できる。`LIST-011`。プレイリストウィンドウだけで複数ドロップを受け、mp4/wmvを入力順に追加し、非対応は除外件数を案内する。対応・非対応混在の実WPFドロップで確認。完了、Codex、2026-08-13。
- [x] `LIST-006` 動画未選択時も、ダイアログで選択した複数ファイルを一括追加できる。`LIST-012`。OS標準ダイアログを`Multiselect=true`、存在確認あり、mp4/wmvフィルターで実装し、ドロップ・現在動画と共通の検証済み追加経路へ接続。完了、Codex、2026-08-13。
- [x] `LIST-007` 同じ動画を複数回登録し、各項目を独立した再生順として扱える。`LIST-004`, `Q-014`。Repository・入力分類・表示の全段で重複を除去せず、実WPFで同一動画を現在動画とドロップから追加して4登録を保持し、JSON再読込を確認。完了、Codex、2026-08-13。
- [x] `LIST-008` ドラッグ操作で項目を並べ替えられる。`LIST-013`。項目の上半分を前、下半分を後の挿入位置として扱い、ファイル名付き半透明ゴーストと青い挿入線を表示して即時保存する。実WPFのドロップ経路と再読込後の順序を確認。完了、Codex、2026-08-13。
- [x] `LIST-009` 選択項目をリストから削除し、元ファイルは削除しない。`LIST-014`。Extended複数選択を登録インデックスで削除し、重複登録を個別に扱う。実WPFで2登録の削除、元動画の存在、再読込後の3件を確認。完了、Codex、2026-08-13。
- [x] `LIST-010` 「再生」操作では常に先頭の再生可能な項目から連続再生を開始する。`LIST-020`, `LIST-022`, `Q-015`。選択状態に依存せず0番から候補探索し、欠損・読込不能候補を飛ばして最初に開けた登録を再生中表示する。実WPF・LibVLCで確認。完了、Codex、2026-08-13。
- [x] `LIST-011` 項目のダブルクリックでは、その項目から連続再生を開始する。`LIST-022`, `Q-015`。重複登録をインデックスで区別し、ダブルクリックした登録から開始する。実WPFで末尾の重複登録から開始して末尾停止することを確認。完了、Codex、2026-08-13。
- [x] `LIST-012` 正常終了後に次の再生可能項目へ進み、欠損・読込エラー項目をスキップする。`LIST-021`, `LIST-023`, `Q-015`。LibVLCのEndReachedを停止・一時停止と分離して通知し、欠損は事前スキップ、読込失敗は利用者エラーを出さず診断ログへ記録して次候補へ進む。実WPFで自然終了3回、重複登録への遷移、末尾停止を確認。完了、Codex、2026-08-13。
- [x] `LIST-014` ループトグルを表示し、末尾でOFFなら停止、ONなら先頭へ戻る。`LIST-024`, `Q-015`。保存済みループ状態を自然終了時の候補探索へ適用し、ONでは末尾から先頭へ一周だけ探索する。実WPF・LibVLCでOFF時の末尾停止とON時の先頭復帰を確認。完了、Codex、2026-08-14。
- [x] `LIST-015` メインウィンドウから別動画を開いた場合は連続再生を解除する。`LIST-025`, `Q-015`。メニュー、最近開いたファイル、起動引数・IPC、メインウィンドウドロップの共通オープン経路で現在登録と読込失敗表示を解除し、プレイリスト内部の候補オープンとは分離した。実WPFで別動画をドロップして自然終了後もプレイリストが再開しないことを確認。完了、Codex、2026-08-14。
- [x] `LIST-016` ループは既定OFFとし、ON/OFF状態を保存して次回起動時に復元する。`LIST-026`, `Q-015`。`playlist.json`へ変更直後に原子的保存し、実WPFでON復元、OFF保存、ウィンドウ再表示と再読み込み後OFFを確認。完了、Codex、2026-08-13。
- [x] `LIST-017` 再生可能項目がない場合はループせず停止し、一度だけ案内する。`LIST-027`, `Q-015`。ループONでも候補探索を一周へ制限し、全候補の欠損・読込失敗後は現在登録を解除して単一の非モーダル案内で停止する。実WPFで読込不能項目だけの一覧が再試行せず停止することを確認。完了、Codex、2026-08-14。

## 13. 最近開いたファイル

- [x] `RECENT-001` 「ファイル」内に最近開いたファイルを最大10件表示する。`RECENT-001..002`。最大10件を最新順で承認済みメニューへ表示し、動的項目はメニュー標準配色を継承する。実WPFで存在2件・欠損3件の表示を確認。完了、Codex、2026-08-13。
- [x] `RECENT-002` 再生開始に成功したファイルだけを記録し、同じファイルは重複させず先頭へ移動する。`RECENT-003`, `Q-016`。正規化絶対パスを大文字小文字非区別で重複排除し、最新を先頭へ移して最大10件を`data/recent-files.json`へ原子的保存する。実製品EXEで初回MP4と二次WMVだけが記録され、0引数・複数引数・欠損MP4では変化しないことを確認。2026-08-12。
- [x] `RECENT-003` 欠損項目を無効表示し、個別に削除できる。欠損項目が2件以上なら、欠損項目だけをすべて履歴から削除できる。`RECENT-004`, `Q-016`。欠損名を明示して無効化し、個別削除と2件以上での一括削除を原子的保存へ接続。実WPFで欠損3件から個別1件、残り2件の一括削除を確認。完了、Codex、2026-08-13。
- [x] `RECENT-004` 履歴項目から動画を開いて自動再生できる。`OPEN-001`, `RECENT-001`。既存の排他的オープン経路へ接続し、再生成功時に履歴先頭へ移動することを実WPF・LibVLCで確認。完了、Codex、2026-08-13。
- [x] `RECENT-005` 履歴をすべて消去できる。`RECENT-004`, `Q-016`。全消去を原子的保存へ接続し、空履歴表示と再読み込み後0件を実WPFゲートで確認。完了、Codex、2026-08-13。

## 14. 性能・品質・配布

- [x] `PERF-001` Phase 2で基準PC、代表動画、測定手順を固定し、候補技術を実測して目標値を決める。`PERF-001`, `PERF-002`, `SPIKE-009`, `Q-019`。2026-08-01にWPF実出力の3条件×5回測定が15/15回合格し、開発者承認により性能目標を確定。
- [x] `PERF-002` プロセスコールドで、プロセス開始から操作可能なメインウィンドウまで1500 ms以内を各5回すべて満たす。製品WPFの代表3条件×5回で最大1,060.416 ms、15/15回合格。2026-08-09。
- [x] `PERF-003` オープン要求から映像・音声準備まで1000 ms以内を各5回すべて満たす。製品WPFでVoutと再生時計100ms到達の遅い方を測定し、最大807.766 ms、15/15回合格。音声経路の実出力証跡はPhase 2を維持。2026-08-09。
- [x] `PERF-004` 50%・90%シーク要求からシーク先到達まで1500 ms以内を各5回すべて満たす。製品シークバー経由で50%最大933.536 ms、90%最大998.667 ms、30/30操作合格。2026-08-09。
- [x] `PERF-005` サムネイル生成と保存処理を再生・UI操作とは別の実行経路で処理し、動画操作をブロックしない。`PERF-003`。別MediaPlayerの逐次ワーカー、50ms協調待機、セッション・優先要求各1件上限とし、UIは要求後即時復帰して200msタイマーでキャッシュ到着だけを監視する。Phase 2の4K併走性能合格と、製品WPFのMP4再生・優先表示・正常終了で確認。完了、Codex、2026-08-15。
- [x] `PERF-006` 速度・音量のバックエンド値が要求から100 ms以内に反映されることを各5回すべて満たす。`PERF-002`。完了、Codex、2026-08-11。製品WPFのUIイベントから実LibVLCバックエンド値までをミュート固定で測定し、MP4は速度最大0.456 ms・音量最大0.281 ms、WMVは速度最大0.427 ms・音量最大0.301 msで各5/5回合格。
- [x] `QUAL-001` 再生バックエンド、永続化、UIを分離し、モックによるUIレビューと各ロジックの単体テストを行える。`QUAL-001`。独立したInfrastructure/Playback/WPF UI構成、`IPlaybackBackend`注入、フェイク実装への交換テスト、製品UIへのLibVLC実装注入を完了。2026-08-09。
- [x] `QUAL-002` 状態遷移、値変換と境界、永続化、プレイリスト、履歴、破損データ復旧の自動テストが通る。`QUAL-003`。Release構成で全243テストが合格し、設定・動画プロファイル・最近開いたファイル・プレイリストの不正文書復旧を含む対象領域を確認。完了、Codex、2026-08-15。
- [x] `QUAL-003` 代表的なmp4/wmv、破損、非対応、欠損ファイルで手動検証する。保証対象MP4/WMVの製品WPF再生、HEVCの製品UI拒否と既存再生維持、欠損MP4の製品EXE起動に加え、偽装MP4の事前拒否・既存状態維持と、公称5秒の途中切断を942 msで再生中エラーへ遷移する経路を実製品WPF・ミュート固定で確認した。途中切断では画面内エラー、再生・シーク無効、診断ログ、最後の時刻維持、`PlaybackEnded` 0回、音声underrun・overflow・失敗0、終了後ワーカー停止を確認。通常5秒MP4は自然終了して誤検出しない。完了、Codex、2026-08-15。
- [x] `QUAL-004` クリーンなWindows 11環境での配布物確認は対象外とする。別環境用の自動・対話受け入れ手順と同一PCの自動事前検証は整備済みだが、開発者が2026-08-15に今後も別PC/VM確認を不要と確定したため、未完了ゲート、残存リスク、将来の追加確認として追跡しない。手順は任意の参考資料として維持する。
- [x] `QUAL-005` ライセンス表記と再現可能なビルド・配布手順を用意する。`v0.1.0`の公開物でLGPL/.NET通知、全ファイルSHA-256台帳、GPL-only除外、319プラグイン監査、x64資産525/525件の来歴、VLC本体・contrib 126件・LibVLCSharp・パッケージングソースを含む対応ソースZIPを再検証。同じGitHub Releaseへバイナリと対応ソースを掲載し、GitHub digestと再ダウンロード後SHA-256の一致を確認。2026-08-09。

## 15. 初回リリース判定

- [x] `REL-001` `specs.md` 2.1で定義した初回最小リリース対象が完了している。クリーン環境確認は開発者承認により免除し、初回後の機能は本判定に含めない。2026-08-09。
- [x] `REL-002` 自動テストがすべて成功している。locked restore、Releaseビルド警告0・エラー0、45/45テスト合格。2026-08-09。
- [x] `REL-003` 製品WPF実ウィンドウでMP4/WMVのオープン・自動再生、再生・一時停止・再開、シーク境界、時刻表示、HEVC拒否と既存再生維持、最終配布フォルダーの起動・通常終了を確認。2026-08-09。
- [x] `REL-004` 性能目標を満たしている。製品WPFの代表3条件×5回で起動、オープン、50%・90%シークが全件閾値内。2026-08-09。
- [x] `REL-005` 重大な既知不具合はなく、クリーン環境未検証、Windows高コントラスト専用配色なし、保証外形式・環境などの残存範囲を計画・検証書・リリースノートへ記録。2026-08-09。
- [x] `REL-006` 開発者が`v0.1.0`、PR経由の`main`反映、GitHub Release公開を承認。2026-08-09。

## 16. 正式版v1.0.0リリース候補

- [x] `REL1-001` 実装済み機能と残件を監査し、正式版の版番号を`1.0.0`へ決定する。全機能チェックリストが完了し、コード内TODO、未解決GitHub Issue/PRは0件。クリーン別環境確認は開発者判断による対象外であり、SDKを10.0.400へ完全固定した。2026-08-15。
- [x] `REL1-002` SDK 10.0.400でlocked restore、Release build、全自動テストに合格する。locked restore成功、Releaseビルド警告0・エラー0、253/253テスト合格。候補EXEの`FileVersion 1.0.0.0`、`ProductVersion 1.0.0+3e664ec...`を確認。2026-08-15。
- [x] `REL1-003` framework-dependent配布フォルダーとZIPを生成し、製品版、ファイル台帳、依存、319 LibVLCプラグイン、GPL-only除外、ライセンス文書、対応ソースZIPを再検証する。commit `3e664ec`のクリーンツリーからSDK 10.0.400で候補を生成し、444ファイル、104,966,758 bytes、プラグイン319件、読取不能・禁止0、必須文書欠損0、対応ソース131エントリを確認。バイナリZIP SHA-256は`F94214C8...2EA9482`、対応ソースは既知の`E72CC13E...27F29`と一致。2026-08-15。
- [x] `REL1-004` 最終配布フォルダーの製品EXEを起動・通常終了し、既存の実WPF・実音声・性能・異常系証跡との対応と残存リスクを正式版検証書へ集約する。候補ZIPを別フォルダーへ展開し、`FileVersion 1.0.0.0`、メインウィンドウ生成・応答、通常終了コード0を確認。製品WPF性能はミュート固定の代表3条件×5回を再測定し、2回目の同一条件測定で15/15回合格、起動最大838.863 ms、オープン・音声準備最大573.647 ms、50%シーク最大899.741 ms、90%シーク最大987.012 ms。初回測定の2件超過も検証書へ記録。2026-08-15。
- [x] `REL1-005` 公開対象commit `74b193e`へ`v1.0.0`タグを付け、GitHub Releaseを通常公開した。バイナリZIP 48,252,945 bytes・SHA-256 `03AD048F...F63531`と、対応ソースZIP 450,885,559 bytes・SHA-256 `FFC79912...F20BFE`を同じReleaseへ掲載し、GitHub digestと再ダウンロード後SHA-256が両方一致した。PR #4でPhase 4実装、PR #5で公開モードのNAudio MIT本文照合にあったCRLF依存を`main`へ反映。完了、Codex、2026-08-15。

## 17. v1.0.0候補レビュー指摘

- [x] `RC1-001` 開発者確認で音声が聞こえなかった原因を特定し、既定の非ミュート動画でWASAPI出力とUI表示が一致することを確認する。候補版の製品プロセスでは、PCM callback後もLibVLCのpause状態が残ってNAudioのActiveなWASAPIレンダーセッションが作られない経路を確認した。PCM play通知を再生可能状態の根拠として残留pauseを解除し、WASAPI初期化時はアプリ内PCM状態を正としてプロセス既定セッションを100%・非ミュートへ同期する。最終候補commit `7e70a0b`の10%製品再生は`スピーカー (USBAudio2.0)`上にActiveセッション1件、100%・非ミュート、30/30ピーク非ゼロ、最大0.095261、終了コード0。Inactive・ミュートの別セッションは出力ピーク0であり、ActiveなNAudioセッションと分離して判定した。実WPFの一時停止・シーク・速度・終了処理もPCM/WASAPI失敗0、276/276テスト合格。開発者が候補版の音声出力を確認。完了、Codex、2026-08-15。
- [x] `RC1-002` シークサムネイル幅をメインウィンドウ幅の5～30%、1%刻み、既定15%として設定・保存し、60～320pxへ制限する。高さは再生中動画の画素寸法・サンプルアスペクト比・回転方向から求めた表示アスペクト比へ追従し、設定変更は現在動画へ即時反映する。初回の10～30%、120～320px実装は実WPF検証に合格したが、10%でも大きいという開発者確認を受けて最小値を再調整した。5%の保存・復元、4%・0.5%刻みの拒否、最小ウィンドウ幅での60px下限、既存10～30%設定、320px上限をテストで固定し、Releaseビルド警告0・エラー0、279/279テスト合格。commit `8f7e0fe`からFileVersion 1.0.0.0のレビュー候補4を生成し、開発者がv1.0.0候補として承認。完了、Codex、2026-08-15。
- [x] `RC1-003` 既存動画から別動画を開く事前検査で、一時的な別映像ウィンドウを表示せず既存再生保護を維持する。ステージングMediaPlayerの映像を1×1の破棄用callbackバッファへ固定し、停止後150ms待機、MediaPlayer破棄、バッファ解放の順を維持した。実WPFで2回目のオープン成功、PCM/WASAPI失敗・underrun・overflow 0、終了後ワーカー停止を確認。完了、Codex、2026-08-15。
- [x] `RC1-004` シークバー上でポインターが動いていない間もホバーポップアップを維持し、生成中または生成完了のUI更新によって開いた直後に閉じないことを確認する。候補ログの抽出タイムアウト2件と、失敗した生成Runを200ms更新が閉じる分岐が一致した。生成が完了・失敗して対象画像がない場合はPopupを閉じず、進捗アニメーションを止めて「サムネイルを表示できません」と表示し、MouseLeaveだけで閉じる状態へ修正。失敗・完了・生成中・取得済み・旧世代を6テストで固定し、実WPFの通常ホバーは271.1msで実画像表示、左右クランプ、音声失敗0に合格。開発者確認で即時消失は再現しなくなった。完了、Codex、2026-08-15。

## 18. 将来バージョン候補

- [x] `NEXT-FULL-001` `v1.0.1`で、`PlaybackControls`自身へ固定ダークテーマの`TextBrush`を明示し、通常WindowからLibVLCSharp `VideoView`の前景オーバーレイへ付け替えても時刻、音量、速度の実効色を`#FFF2F6FC`で維持する。版を`1.0.1`へ更新し、Releaseビルド警告0・エラー0、279/279自動テストに合格。実WPFでは通常・全画面の色一致、3秒自動非表示、操作中表示、通常復帰、96/144 DPIの3画面での全画面境界・復帰位置を確認。公開モード候補は445ファイル、`FileVersion 1.0.1.0`、319 LibVLCプラグイン、読取不能・禁止0、ZIP SHA-256 `F74EB8F5...D25E86`を確認。完了、Codex、2026-08-16。
- [x] `NEXT-SEEK-001` `v1.0.1`で再生位置スライダーのXAML初期ツールチップと状態更新時のツールチップ設定を削除し、未選択、読込中、再生中、エラー時に標準ツールチップを表示しない。シークサムネイルのMouseMove/MouseLeave処理と`AutomationProperties.Name="再生位置"`は維持した。Releaseビルド警告0・エラー0、279/279自動テストに合格し、実WPFで未選択時・再生中とも`ToolTip`未設定を確認。公開モード候補は445ファイル、`FileVersion 1.0.1.0`、319 LibVLCプラグイン、読取不能・禁止0、ZIP SHA-256 `F74EB8F5...D25E86`。完了、Codex、2026-08-16。
- [x] `NEXT-REL-001` `v1.0.1`のリリースノートを作成し、commit `aab9867`のクリーンツリーからlocked restore、Release build警告0・エラー0、279/279テスト、公開モード配布ZIP、319 LibVLCプラグインとGPL-only除外、対応ソース131エントリ再照合、候補EXE起動・通常終了に合格。バイナリZIPは48,252,605 bytes・SHA-256 `F74EB8F5...D25E86`、対応ソースZIPは450,885,559 bytes・SHA-256 `FFC79912...F20BFE`。公開、タグ、GitHub Releaseは開発者承認後に分離する。完了、Codex、2026-08-16。
- [x] `NEXT-REL-002` `v1.0.1`ブランチをPR #7・merge commit `9c37b2f`として`main`へ反映し、同commitからlocked restore、Release build警告0・エラー0、279/279テスト、公開用候補を再生成した。`v1.0.1`タグと通常GitHub ReleaseへバイナリZIP 48,252,551 bytes・SHA-256 `36F94F02...D6BA92`、対応ソースZIP 450,885,559 bytes・SHA-256 `FFC79912...F20BFE`を掲載し、GitHub digestと再ダウンロード後SHA-256が両方一致した。完了、Codex、2026-08-16。
- [x] `NEXT-UPD-001` `v1.1.0`で、GitHubの最新の公開済み正式Releaseを確認し、現在のSemVerより上位かを表示できるようにする。draft・prereleaseは対象外とし、起動時は最後の自動確認試行から24時間以上なら確認、メニューからはいつでも手動確認する。通信失敗、API制限、不正な版、対象asset欠損時も現在版を継続利用できること。2026-08-16仕様承認。Release確認中核、24時間制御、試行時刻保存、起動後の自動確認、手動確認メニュー、通知・概要表示、終了時取消を実装し、333/333テスト合格。public化後の匿名実APIで`v1.0.1`・対象ZIP・size・digestを確認した。専用の実WPFゲートでメニュー表示、自動更新通知、UTC試行時刻の再読込、手動最新通知、保留中通信の終了時取消と取消診断を確認。完了、Codex、2026-08-23。
- [x] `NEXT-UPD-002` 上位版の`BakuretsuOsakanaKobo-win-x64.zip`を一時領域へダウンロードし、GitHub Release assetのsizeとSHA-256 digestを照合してから置換準備へ進む。対象リポジトリ・asset名を固定し、不一致、破損ZIP、容量不足、取消時は現行版を変更しない。2026-08-15要求。ZIP 512 MiB・展開後合計1 GiB・manifest 1 MiBの上限、取得前後の空き容量確認、manifestとZIP全ファイルの安全なパス・size・SHA-256照合、`data`・リンク・Windows予約名拒否、失敗・取消時の専用一時領域除去を実装した。351/351テスト合格。公開`v1.0.1`の48,252,551 bytes・SHA-256 `36F94F02...D6BA92`を実取得し、展開後104,979,800 bytes、444ファイル全件、実行ファイルあり、`data` 0件を確認。完了、Codex、2026-08-24。
- [x] `NEXT-UPD-003` 利用者の明示確認後、別プロセスの更新ヘルパーで本体終了後にアプリ所有ファイルだけを置換し、`data`を変更しない。置換前バックアップ、失敗・中断時ロールバック、書き込み不能時の手動更新案内を備え、成功後は新しい本体を再起動する。2026-08-16仕様承認。独立トランザクション核、Updaterホスト、アプリ側の明示同意・ダウンロード・ready後通常終了、結果案内・清掃を実装した。複製配布物の実Updaterで親終了前の旧版維持、正常置換後の新版再起動、完全ロールバック後の旧版再起動、`data`・未知ファイル保全、結果読込、作業領域清掃に合格した。実WPFでは確認ダイアログの版・概要・終了・再起動案内、「いいえ」でダウンロード0・現行ウィンドウ維持、「はい」でダウンロード表示、ready前のウィンドウ維持、要求検証後の通常終了を確認した。Releaseビルド警告0・エラー0、374/374テスト、既存実WPF更新確認・終了取消ゲート、ValidationOnly製品発行453ファイル・Updater 7ファイル・`data` 0・LibVLCプラグイン319件・読取不能禁止0を維持。完了、Codex、2026-08-28。
- [x] `NEXT-UPD-004` `v1.1.0`更新機能について、版比較、24時間間隔、明示確認、取消、通信・API制限、manifest、digest、ZIP、容量、権限、本体終了待ち、置換、ロールバック、再起動の自動テストと、配布フォルダーを複製した実更新・復旧検証を行う。通常の依存・ライセンス・対応ソース公開ゲートも維持する。権限拒否時の安全な縮退テストと要件・証跡対応表を補い、一括ゲートで375/375テスト、実WPF、実Updater正常更新・完全復旧、ValidationOnly候補453ファイル、319プラグイン、公開`v1.0.1` ZIP再取得に合格。完了、Codex、2026-08-29。
- [x] `NEXT-START-001` 初回起動および終了から時間を置いたコールド相当起動について、プロセス開始、Window生成、設定・動画プロファイル・履歴・プレイリスト読込、LibVLC、サムネイルサービス、`Show`、操作可能までの区間を複数回計測する。空起動とファイル引数起動を分け、既存1500 ms基準だけでなく利用者が待つ区間と主要因を特定する。実製品3条件×5回で反復時の操作可能中央値1,101.70～1,191.34 msを確認し、新規発行先の初回はLibVLC初期化11,780.21 msにより13,280.22 msとなることを再現した。完了、Codex、2026-08-29。
- [x] `NEXT-START-002` 計測結果に基づき、操作可能なウィンドウ表示に不要な永続データ・LibVLC・サムネイル・更新確認を遅延または安全に並列化する。空起動の表示を優先しつつ、起動引数・IPCの順序、エラー通知、再生開始、終了時の初期化取消・破棄、既存性能を回帰させず、改善後の数値目標を開発者レビューで確定する。空シェル先行表示、表示後の4 Repository並列読込・LibVLCバックグラウンド初期化、FIFO要求処理、初期化中終了を実装した。実製品3条件×5回のシェル最大1,238.20 ms、反復時完全初期化最大1,110.30 ms、15/15正常終了に合格。新規発行先初回の完全初期化約12.44秒はシェルをブロックしない。開発者がシェル・反復時完全初期化の各1,500 ms目標と、初回再生機能の遅延有効化を承認。完了、Codex、2026-08-30。
- [x] `NEXT-REL11-001` `v1.1.0`のリリースノートを作成し、commit `292e162`のクリーンツリーからlocked restore、Release build警告0・エラー0、377/377テスト、更新・実Updater・起動性能ゲート、公開モードのframework-dependent win-x64配布453ファイル、319 LibVLCプラグイン・読取不能0・禁止0、525ネイティブ資産の来歴、対応ソース131エントリ、`FileVersion 1.1.0.0`、候補EXEの応答・通常終了を再検証した。バイナリZIPは48,481,401 bytes・SHA-256 `7444527D...ABE0BAD`、Releaseごとに再生成した対応ソースZIPは450,885,559 bytes・SHA-256 `7CCD06B6...EF55EBE`。タグ・GitHub Release公開は開発者承認後へ分離する。完了、Codex、2026-08-30。
- [ ] `NEXT-REL11-002` 開発者が`v1.1.0`候補を承認した後、PR経由で`main`へ反映し、公開対象commitから候補を再生成する。`v1.1.0`タグと通常GitHub ReleaseへバイナリZIP・対応ソースZIPを掲載し、GitHub asset digestと再ダウンロード後のbytes・SHA-256を照合する。公開`v1.0.1`から`v1.1.0`を検出・取得できることも確認する。未着手、2026-08-30。
- [ ] `NEXT-LIST-001` `v1.2.0`で名前と安定IDを持つ複数プレイリストを管理できるようにする。作成・切替・名前変更・削除UI、項目順、プレイリスト単位のループ状態、既存の単一`playlist.json`からの後方互換な移行方式を実装前に仕様・UIレビューする。連続再生中の切替では現在動画を維持し、切替前リストの連続再生だけを解除する。2026-08-16仕様承認。
- [ ] `NEXT-LIST-THUMB-001` プレイリスト項目へ動画の永続サムネイルを表示し、動画面の右クリックメニューから現在フレームを登録・置換できるようにする。既存シークサムネイルのセッションキャッシュとは分離し、未登録・欠損・読込不能・破損画像では安全なプレースホルダーを表示する。動画パス単位で重複登録・複数プレイリスト間に共有し、`data`配下へ画像本体を保存する案を推奨。`v1.2.0`候補、2026-08-18要求。
- [ ] `NEXT-LIST-THUMB-002` サムネイルの所有単位、登録解除、画像形式・寸法・ファイル名、原子的保存、旧画像削除、書込不能、動画移動・改名、プレイリスト行レイアウト、DPI、長い一覧での遅延読込とメモリ上限を、実装前の仕様・UIレビューで確定する。2026-08-18追加。
